import React from 'react';

const Footer = () => {
  const footerStyle = {
    backgroundColor: 'white',
    padding: '10px',
    height: '100px',
    textAlign: 'center',
    color: 'black', // Optional: Adjust text color if needed
    borderTop: '1px solid #ddd', // Optional: Add a subtle border for a cleaner look
  };

  return (
    <footer style={footerStyle}>
      <p></p>
    </footer>
  );
};

export default Footer;
